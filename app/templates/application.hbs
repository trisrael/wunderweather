<screen>
  <page>
  <vbox>
    {{#each model as |test|}}
    <box>
      {{#paper-checkbox value=value1 onChange=null}}
        A checkbox:
      {{/paper-checkbox}}
      {{test.name}}
    </box>
    {{/each}}
  </vbox>

{{outlet}}
</page>
</screen>
